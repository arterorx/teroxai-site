---
import AboutPage from '../../components/AboutPage.astro';
import { OTHER_LOCALES } from '../../i18n/config';

export function getStaticPaths() {
  return OTHER_LOCALES.map((lang) => ({ params: { lang }, props: { lang } }));
}
const { lang } = Astro.props;
---

<AboutPage lang={lang} />
